#T.Pham
import os
import sys
import const
import dbConector

#working
class Review(object):
	
	
	def __init__(self, reviewID="", userID="", userRating=0, userThumbs=True):
		
		self.reviewID = reviewID
		self.userID = userID
		self.userRating = rating
		self.userThumbs = thumbs
		
		
		def getReview(self):
		review = [self.reviewID,self.userID
		